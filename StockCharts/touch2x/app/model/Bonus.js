Ext.define("Chartsly.model.Bonus", {
    extend: 'Ext.data.Model',
    config: {
        fields: [
            'date',
            "type",
            "bonus",
            "remark"
        ]
     }
});
